import {Component} from '@angular/core';
import 'hammerjs';

@Component({
  moduleId: module.id,
  selector: 'app-root',
  templateUrl: 'app.html',
  styleUrls: ['app.css'],
})
export class AppComponent {
}
